# Course-protocol modification — data linkage

`CourseProtocol_AppendixA_DataLinkage_MARKED.docx` — a drafted, **not submitted**
modification to the already-approved course-based protocol
(`../inputs/approved-protocols/2024_08_09 Nelson_MOD_2_Jan_2026_FINAL.pdf`).

It adds optional participant consent to link their data across the team's related
reflection-group studies, which the grant needs for cross-task analysis. Backlog item **7**.

This is a modification to an *existing approved* protocol, so it does not belong to IRB #1 or
IRB #2 — it travels separately, which is why it sits in its own folder. It is also the source
of the cross-study linkage language reused in the new protocols; IRB #1's
`FINAL_structured` candidate already carries a version of that paragraph.

`MARKED` means it is a tracked-changes / marked-up view against the approved appendix, not a
clean document.
